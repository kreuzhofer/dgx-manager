{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -q 'packages/agent/src/'; then cd \"$CLAUDE_PROJECT_DIR\" && ./scripts/bump-agent-version.sh; fi"
          }
        ]
      }
    ]
  }
}
